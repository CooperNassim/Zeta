# 交易编号生成规则修改

## 修改日期
2026-03-22

## 问题描述
之前的交易编号生成逻辑中,买入和卖出订单都会生成新的交易编号,这导致同一笔交易的买入和卖出记录有不同的交易编号,不利于追踪和管理。

## 修改内容
修改了 `src/store/useStore.js` 中的 `addOrder` 函数,实现了以下逻辑:

### 修改后的交易编号生成规则

1. **买入订单**
   - 调用 `generateTradeNumber()` 生成新的交易编号
   - 格式: `YYYYMMDDNNN` (如: 20260322001)

2. **卖出订单**
   - 如果关联了买入订单(有 `buyOrderId`),则**继承**该买入订单的交易编号
   - 如果没有关联买入订单,则生成新的交易编号

### 代码逻辑
```javascript
// 卖出订单: 如果关联了买入订单,继承该买入订单的交易编号
// 否则生成新的交易编号
let tradeNumber
if (order.type === 'sell' && order.buyOrderId) {
  // 从买入订单获取交易编号
  const buyOrder = state.orders.find(o => o.id === order.buyOrderId)
  if (buyOrder && buyOrder.tradeNumber) {
    tradeNumber = buyOrder.tradeNumber
  } else {
    tradeNumber = state.generateTradeNumber()
  }
} else {
  // 买入订单或不关联买入的卖出订单,生成新交易编号
  tradeNumber = state.generateTradeNumber()
}
```

## 影响范围

### 前端影响
1. **订单管理页面** (`src/pages/OrderManagement.jsx`)
   - 卖出订单会自动继承买入订单的交易编号
   - 在列表中按交易编号分组时,同一笔交易的买入和卖出会自动归为一组

2. **交易记录页面** (`src/pages/TradeRecords.jsx`)
   - 交易记录基于订单生成,因此也会遵循相同的交易编号规则
   - 同一笔交易的买入和卖出记录会有相同的 `tradeNumber`

### 数据库影响
1. **trade_orders 表**
   - `trade_number` 字段会正确存储继承的交易编号
   - 同一笔交易的买入和卖出订单会有相同的 `trade_number`

2. **trade_records 表**
   - `trade_number` 字段会正确存储继承的交易编号
   - 同一笔交易的买入和卖出记录会有相同的 `trade_number`

## 使用场景

### 场景1: 创建买入订单
```
1. 用户点击"买入"按钮
2. 系统生成交易编号: 20260322001
3. 创建买入订单,交易编号为 20260322001
```

### 场景2: 创建卖出订单(关联买入订单)
```
1. 用户勾选买入订单(交易编号: 20260322001)
2. 点击"卖出"按钮
3. 系统自动继承交易编号: 20260322001
4. 创建卖出订单,交易编号为 20260322001
5. 同一笔交易的买入和卖出有相同的交易编号
```

### 场景3: 创建独立卖出订单
```
1. 用户直接点击"卖出"按钮(未勾选买入订单)
2. 系统生成新的交易编号: 20260322002
3. 创建卖出订单,交易编号为 20260322002
```

## 优势

1. **数据一致性**: 同一笔交易的买入和卖出有相同的交易编号
2. **易于追踪**: 可以通过交易编号快速找到完整的交易记录
3. **分组显示**: 在列表中按交易编号分组时,同一笔交易的记录会自动聚合
4. **统计便利**: 可以按交易编号计算每笔交易的盈亏

## 注意事项

1. **历史数据**: 修改前的历史订单和交易记录的交易编号可能不一致,需要手动处理
2. **数据迁移**: 如果需要处理历史数据,可以编写迁移脚本统一交易编号
3. **业务逻辑**: 确保前端页面正确传递 `buyOrderId` 参数,以便卖出订单能够正确继承交易编号

## 相关文件

- `src/store/useStore.js` - 交易编号生成逻辑
- `src/pages/OrderManagement.jsx` - 订单管理页面
- `src/pages/TradeRecords.jsx` - 交易记录页面
